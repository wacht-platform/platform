{
  "system_instruction": {
      "parts": [
        {
          "text": "{{system_prompt}}"
        }
      ]
   },
  "contents": [
    {{#each conversation_history}}
    {
      "role": "{{this.role}}",
      "parts": [
        {
          "text": "{{this.content}}"
        }
      ]
    }{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "generationConfig": {
    "thinkingConfig": {
      "thinkingBudget": 512
    },
    "responseMimeType": "application/json",
    "responseSchema": {
      "type": "OBJECT",
      "properties": {
        "task_execution": {
          "type": "OBJECT",
          "properties": {
            "approach": {
              "type": "STRING",
              "description": "How you plan to execute this specific task"
            },
            "actions": {
              "type": "OBJECT",
              "properties": {
                "action": {
                  "type": "ARRAY",
                  "items": {
                    "type": "OBJECT",
                    "properties": {
                      "type": {
                        "type": "STRING",
                        "enum": ["tool_call", "workflow_call"],
                        "description": "Type of action - either tool_call for specific tools or workflow_call for complex workflows"
                      },
                      "details": {
                        "type": "OBJECT",
                        "description": "Action-specific details including parameters",
                        "properties": {
                          "tool_name": {
                            "type": "STRING",
                            "description": "Name of the tool to use (for tool_call type)"
                          },
                          "workflow_name": {
                            "type": "STRING", 
                            "description": "Name of the workflow to use (for workflow_call type)"
                          },
                          "description": {
                            "type": "STRING",
                            "description": "Description of what this action will do"
                          },
                          "parameter_hints": {
                            "type": "STRING",
                            "description": "Hints about what parameters will be needed (not actual values)",
                            "nullable": true
                          }
                        },
                        "required": ["description"]
                      },
                      "purpose": {
                        "type": "STRING",
                        "description": "Why this action helps complete the task"
                      }
                    },
                    "required": ["type", "details", "purpose"]
                  }
                }
              },
              "required": ["action"]
            },
            "expected_result": {
              "type": "STRING",
              "description": "What result you expect from this execution"
            }
          },
          "required": ["approach", "actions", "expected_result"]
        },
        "execution_status": {
          "type": "STRING",
          "enum": ["ready", "blocked", "cannot_execute"],
          "description": "Whether the task can be executed now"
        },
        "blocking_reason": {
          "type": "STRING",
          "description": "If blocked or cannot execute, explain why",
          "nullable": true
        }
      },
      "required": ["task_execution", "execution_status"]
    },
    "temperature": 0.5,
    "maxOutputTokens": 4096
  }
}