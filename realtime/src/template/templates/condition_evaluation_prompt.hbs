Evaluate this condition based on the given context:

## Condition to Evaluate:
{{condition}}

## Current Context:
{{context_info}}

## Instructions:

Analyze the condition against the provided context and determine if it evaluates to true or false.

Consider:
- Variable values and their types
- Logical operators and comparisons
- String matching and patterns
- Numerical comparisons
- Boolean logic
- Null/undefined checks

## Response Format:

Respond with ONLY one of the following:
- `true` - if the condition is met
- `false` - if the condition is not met

## Examples:

Context: `{"user_count": 5, "status": "active", "premium": true}`

- Condition: "user_count > 3" → `true`
- Condition: "status equals inactive" → `false`
- Condition: "premium is true" → `true`
- Condition: "user_count less than 10 and status is active" → `true`

## Natural Language Patterns:

The condition may use natural language patterns like:
- "X is greater than Y"
- "X equals Y" 
- "X contains Y"
- "X is true/false"
- "X and Y"
- "X or Y"
- "not X"

Evaluate the condition now:
